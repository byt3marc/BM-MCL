[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bm-mcl"
version = "0.1.0"
description = "Minecraft launcher"
requires-python = ">=3.11"
dependencies = [
    "minecraft-launcher-lib>=8.0,<9",
]
